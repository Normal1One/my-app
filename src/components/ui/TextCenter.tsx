const TextCenter = ({ text }: { text: string }) => {
    return (
        <div className='absolute top-1/2 w-full text-center text-lg'>
            {text}
        </div>
    )
}

export default TextCenter
